language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-devroids' ]; then cd .. && eval "mv $currentfolder generator-devroids" && cd generator-devroids; fi

